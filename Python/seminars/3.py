# Задайте последовательность чисел. Напишите программу, 
# которая выведет список неповторяющихся элементов исходной последовательности.
# origins=[]
# target = seq[0]
# for i in range (a):

# цикл в цикле 1 по элементам 2 по уменьшающемуся списку со счетчиком


# def search (seq,tar):
#     for i in range(len(seq)):
#         if seq[i]==tar:
#             return i
#     return -1

# def return_distinct (seq):
#     for j in range (len(seq)):
#         tar=seq[j]
#         res=search(seq[j+1:],tar)
#         if res!=-1:
#            seq.pop(j)
#            seq.pop(res)
#     return seq   